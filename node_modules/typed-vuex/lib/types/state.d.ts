import { Not } from './utilities';
declare type StateObject = Not<Record<string, any>, Function>;
declare type StateFunction = Not<() => unknown | any, Record<string, any>>;
export declare type State = StateObject | StateFunction;
export declare type StateType<T extends State> = T extends () => any ? ReturnType<T> : T;
export {};
